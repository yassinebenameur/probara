apiVersion: v2
name: probara
description: A Helm chart for the Probara monitoring platform
type: application
version: 0.1.0
appVersion: "0.1.0"

# No external dependencies - PostgreSQL and NATS are embedded as templates
